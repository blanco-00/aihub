<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aihub.admin.mapper.DictDataMapper">
    
    <!-- 根据字典类型查询字典数据列表 -->
    <select id="selectDictDataListByType" resultType="com.aihub.admin.dto.response.DictDataResponse">
        SELECT 
            id, dict_type, dict_label, dict_value, sort_order, status, remark,
            created_at as createdAt, updated_at as updatedAt
        FROM dict_data
        WHERE is_deleted = 0
        <if test="dictType != null and dictType != ''">
            AND dict_type = #{dictType}
        </if>
        ORDER BY sort_order ASC, created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    
    <!-- 统计字典数据总数 -->
    <select id="countDictDataListByType" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM dict_data
        WHERE is_deleted = 0
        <if test="dictType != null and dictType != ''">
            AND dict_type = #{dictType}
        </if>
    </select>
    
</mapper>
