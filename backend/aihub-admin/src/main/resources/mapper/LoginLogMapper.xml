<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aihub.admin.mapper.LoginLogMapper">
    
    <!-- 查询登录日志列表（分页、搜索、筛选） -->
    <select id="selectLoginLogList" resultType="com.aihub.admin.dto.response.LoginLogResponse">
        SELECT 
            id, user_id, username, ip, address, user_agent, 
            status, message, login_time
        FROM login_log
        <where>
            <if test="request.username != null and request.username != ''">
                AND username LIKE CONCAT('%', #{request.username}, '%')
            </if>
            <if test="request.ip != null and request.ip != ''">
                AND ip LIKE CONCAT('%', #{request.ip}, '%')
            </if>
            <if test="request.status != null">
                AND status = #{request.status}
            </if>
            <if test="request.startTime != null and request.startTime != ''">
                AND login_time &gt;= #{request.startTime}
            </if>
            <if test="request.endTime != null and request.endTime != ''">
                AND login_time &lt;= #{request.endTime}
            </if>
        </where>
        ORDER BY login_time DESC
        LIMIT #{size} OFFSET #{offset}
    </select>
    
    <!-- 统计登录日志总数 -->
    <select id="countLoginLogList" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM login_log
        <where>
            <if test="request.username != null and request.username != ''">
                AND username LIKE CONCAT('%', #{request.username}, '%')
            </if>
            <if test="request.ip != null and request.ip != ''">
                AND ip LIKE CONCAT('%', #{request.ip}, '%')
            </if>
            <if test="request.status != null">
                AND status = #{request.status}
            </if>
            <if test="request.startTime != null and request.startTime != ''">
                AND login_time &gt;= #{request.startTime}
            </if>
            <if test="request.endTime != null and request.endTime != ''">
                AND login_time &lt;= #{request.endTime}
            </if>
        </where>
    </select>
    
</mapper>
