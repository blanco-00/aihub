spring:
  datasource:
    url: jdbc:mysql://your-prod-host:3306/aihub?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: your_prod_username
    password: your_prod_password
  
  # 生产环境禁用 Flyway（必须）
  flyway:
    enabled: false

logging:
  level:
    com.aihub: info
    root: warn
