<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aihub.mapper.UserMapper">

    <select id="findByUsername" resultType="com.aihub.entity.User">
        SELECT * FROM user 
        WHERE username = #{username} 
        AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="findByEmail" resultType="com.aihub.entity.User">
        SELECT * FROM user 
        WHERE email = #{email} 
        AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="countByRoleAndNotDeleted" resultType="java.lang.Long">
        SELECT COUNT(*) FROM user 
        WHERE role = #{role} 
        AND is_deleted = 0
    </select>

    <!-- 用户列表查询（支持搜索和筛选） -->
    <select id="selectUserList" resultType="com.aihub.dto.UserListResponse">
        SELECT 
            u.id,
            u.username,
            u.email,
            u.role,
            u.status,
            u.created_at as createdAt,
            u.updated_at as updatedAt
        FROM user u
        WHERE u.is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (u.username LIKE CONCAT('%', #{keyword}, '%') 
                 OR u.email LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="role != null and role != ''">
            AND u.role = #{role}
        </if>
        <if test="status != null">
            AND u.status = #{status}
        </if>
        ORDER BY u.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 统计用户总数（支持搜索和筛选） -->
    <select id="countUserList" resultType="java.lang.Long">
        SELECT COUNT(*) 
        FROM user u
        WHERE u.is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (u.username LIKE CONCAT('%', #{keyword}, '%') 
                 OR u.email LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="role != null and role != ''">
            AND u.role = #{role}
        </if>
        <if test="status != null">
            AND u.status = #{status}
        </if>
    </select>

</mapper>
