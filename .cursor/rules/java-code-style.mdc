---
alwaysApply: true
---

# Java 代码规范

## Import 规范

- **禁止使用全限定名**：不得在代码中使用全限定名（Fully Qualified Name），必须通过 `import` 语句导入类
- **原因**：全限定名泛滥会让代码很难读，影响代码可读性和维护性

### 错误示例
```java
// ❌ 错误：使用全限定名
public void createUser(com.aihub.entity.User user) {
    com.aihub.mapper.UserMapper mapper = new com.aihub.mapper.UserMapper();
}
```

### 正确示例
```java
// ✅ 正确：使用 import 语句
import com.aihub.entity.User;
import com.aihub.mapper.UserMapper;

public void createUser(User user) {
    UserMapper mapper = new UserMapper();
}
```

## 代码简洁性

- **代码应该简单清晰易读**：优先选择简单直接的解决方案
- **命名清晰**：使用有意义的变量名和方法名，避免缩写（除非广泛认知）
- **方法长度控制**：单个方法不超过 50 行（特殊情况不超过 100 行）
- **避免深层嵌套**：嵌套层级不超过 3 层，使用早期返回（Early Return）

## 代码抽象与复用

- **尽可能抽象并复用代码**：让代码更优雅
- **提取公共方法**：重复代码超过 3 行，应该提取为公共方法
- **使用工具类**：通用功能应该封装为工具类（静态方法，无状态）
- **适度使用设计模式**：在合适的场景使用设计模式，不要为了使用而过度设计

## 实用主义原则

- **避免炫技而复杂化代码**：以实用为主
- **避免过度设计**：不要为了展示技术而复杂化代码，优先选择简单直接的解决方案
- **优先使用框架特性**：充分利用 Spring、MyBatis 等框架的特性，不要重复造轮子
- **保持代码可读性**：代码应该自解释，减少注释需求；必要的注释应该解释"为什么"，而非"是什么"
