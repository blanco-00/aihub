---
alwaysApply: true
---
# 前端UI/UX设计规范

## 设计原则

- **简洁高效**: 减少认知负担，提高操作效率
- **信息层次**: 重要信息突出，次要信息收敛
- **一致性**: 统一的视觉语言和交互模式
- **可访问性**: 遵循WCAG标准，确保良好的用户体验
- **科技感**: 体现AI项目的科技属性，营造未来感和专业感

## 布局规范

### 布局参考标准
- **必须参考 Element Plus 官方示例的布局方式**：学习官方文档中的布局最佳实践
- **整体布局结构**：
  - Header: Logo + 导航 + 用户信息
  - Sidebar: 菜单导航（可折叠）
  - Main Content: 页面主要内容区域
- **响应式设计**：
  - Mobile: < 768px（侧边栏自动收起）
  - Tablet: 768px - 1024px（自适应布局）
  - Desktop: > 1024px（完整布局）

### 布局实现要求
- 使用 Element Plus 的 `el-container`、`el-header`、`el-aside`、`el-main` 组件
- 保持页面内容区域的内边距统一（通常 16px 或 24px）
- 避免页面内容紧贴边缘，保持适当的留白

### 错误示例
```vue
<!-- ❌ 错误：直接使用 div，没有参考 Element Plus 布局组件 -->
<template>
  <div class="page">
    <div class="header">...</div>
    <div class="content">...</div>
  </div>
</template>
```

### 正确示例
```vue
<!-- ✅ 正确：使用 Element Plus 布局组件，参考官方示例 -->
<template>
  <el-container>
    <el-header>Header</el-header>
    <el-container>
      <el-aside width="200px">Sidebar</el-aside>
      <el-main>
        <!-- 页面内容，保持 16px 或 24px 内边距 -->
      </el-main>
    </el-container>
  </el-container>
</template>
```

## 配色规范

### 必须遵循文档中的色彩系统
- **主色调**:
  - Primary: `#1890ff` (蓝色，科技感)
  - Success: `#52c41a` (绿色，成功状态)
  - Warning: `#faad14` (橙色，警告)
  - Error: `#f5222d` (红色，错误)
- **中性色**:
  - Text Primary: `#262626`
  - Text Secondary: `#8c8c8c`
  - Border: `#d9d9d9`
  - Background: `#fafafa`

### 配色使用要求
- **禁止随意使用颜色**：所有颜色必须来自色彩系统
- **使用 Element Plus 的主题色变量**：通过 CSS 变量或主题配置使用
- **保持色彩一致性**：相同语义使用相同颜色（如所有成功提示都用 Success 色）

### 正确示例
```vue
<!-- ✅ 正确：使用 Element Plus 的 type 属性，自动应用主题色 -->
<el-button type="primary">主要按钮</el-button>
<el-button type="success">成功按钮</el-button>
<el-alert type="warning" :closable="false">警告信息</el-alert>
```

```css
/* ✅ 正确：使用 CSS 变量或主题配置 */
.custom-card {
  border-color: var(--el-border-color);
  background-color: var(--el-bg-color);
  color: var(--el-text-color-primary);
}
```

## 科技感设计规范

### 核心要求
- **必须体现AI项目的科技属性**：通过视觉元素、配色、动画等营造未来感和专业感
- **避免过度设计**：科技感应服务于功能，不应影响可用性
- **保持现代感**：使用现代化的设计语言和交互模式

### 配色增强科技感
- **主色调使用科技蓝**：`#1890ff` 蓝色系，体现科技感和专业性
- **渐变效果**：在关键区域使用蓝色渐变（如按钮、卡片背景）
- **深色模式支持**：提供深色主题选项，增强科技感
- **霓虹效果**：在数据可视化、状态指示等场景使用微妙的发光效果

### 视觉元素
- **几何图形**：使用简洁的几何图形作为装饰元素（如六边形、圆形、线条）
- **网格背景**：在仪表盘等页面使用微妙的网格或点阵背景
- **数据可视化**：使用现代化的图表样式，支持动画效果
- **图标风格**：使用线性图标或填充图标，保持统一风格

### 动画效果
- **微交互**：按钮悬停、卡片悬停使用平滑的过渡动画
- **数据更新动画**：数字变化使用滚动动画（countup效果）
- **加载动画**：使用科技感的加载动画（如粒子效果、脉冲效果）
- **页面过渡**：使用淡入淡出或滑动过渡，保持流畅感

### 布局特点
- **卡片式设计**：使用卡片容器，配合阴影和圆角
- **留白充足**：保持足够的留白，营造呼吸感
- **层次分明**：通过阴影、边框、背景色区分层次
- **响应式网格**：使用网格布局，保持整齐有序

### 科技感实现示例

```vue
<!-- ✅ 正确：科技感卡片设计 -->
<template>
  <el-card class="tech-card" shadow="hover">
    <div class="card-header">
      <h3>Token 消耗统计</h3>
      <el-icon class="tech-icon"><DataAnalysis /></el-icon>
    </div>
    <div class="tech-number">{{ animatedCount }}</div>
    <div class="tech-trend">
      <el-icon :class="trendClass"><TrendCharts /></el-icon>
      <span>+12.5%</span>
    </div>
  </el-card>
</template>

<style scoped>
.tech-card {
  background: linear-gradient(135deg, #f5f7fa 0%, #ffffff 100%);
  border: 1px solid rgba(24, 144, 255, 0.1);
  transition: all 0.3s ease;
}

.tech-card:hover {
  box-shadow: 0 8px 24px rgba(24, 144, 255, 0.15);
  transform: translateY(-2px);
}

.tech-icon {
  color: #1890ff;
  font-size: 24px;
}

.tech-number {
  font-size: 32px;
  font-weight: 600;
  color: #1890ff;
  font-family: 'SF Mono', monospace;
}

.tech-trend {
  display: flex;
  align-items: center;
  gap: 8px;
  color: #52c41a;
}
</style>
```

```css
/* ✅ 正确：科技感背景效果 */
.tech-background {
  background: 
    linear-gradient(135deg, rgba(24, 144, 255, 0.05) 0%, transparent 50%),
    radial-gradient(circle at 20% 50%, rgba(24, 144, 255, 0.1) 0%, transparent 50%),
    #fafafa;
  position: relative;
}

.tech-background::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-image: 
    linear-gradient(rgba(24, 144, 255, 0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(24, 144, 255, 0.03) 1px, transparent 1px);
  background-size: 20px 20px;
  pointer-events: none;
}
```

```vue
<!-- ✅ 正确：科技感按钮设计 -->
<template>
  <el-button 
    type="primary" 
    class="tech-button"
    :icon="Loading"
    :loading="loading"
  >
    开始分析
  </el-button>
</template>

<style scoped>
.tech-button {
  background: linear-gradient(135deg, #1890ff 0%, #096dd9 100%);
  border: none;
  box-shadow: 0 4px 12px rgba(24, 144, 255, 0.3);
  transition: all 0.3s ease;
}

.tech-button:hover {
  box-shadow: 0 6px 16px rgba(24, 144, 255, 0.4);
  transform: translateY(-1px);
}

.tech-button:active {
  transform: translateY(0);
  box-shadow: 0 2px 8px rgba(24, 144, 255, 0.3);
}
</style>
```

### 禁止的设计
- ❌ 避免使用过于花哨的动画，影响性能
- ❌ 避免使用过多的装饰元素，影响内容阅读
- ❌ 避免使用过于鲜艳或刺眼的颜色
- ❌ 避免使用过时的设计风格（如拟物化设计）

## 间距规范

### 统一间距系统
- **基础单位**: `4px`
- **常用间距**: `8px`, `12px`, `16px`, `24px`, `32px`, `48px`
- **间距使用规则**:
  - 元素内部间距：8px、12px
  - 元素之间间距：16px、24px
  - 区块之间间距：24px、32px、48px

### 间距实现要求
- **使用 Element Plus 的间距工具类**：优先使用组件库提供的间距方案
- **保持间距一致性**：相同层级的元素使用相同间距
- **避免随意设置间距值**：所有间距值必须是 4px 的倍数

### 错误示例
```vue
<!-- ❌ 错误：随意设置间距值，不符合间距系统 -->
<template>
  <div style="padding: 13px; margin: 7px;">
    <div style="margin-bottom: 15px;">内容</div>
  </div>
</template>
```

### 正确示例
```vue
<!-- ✅ 正确：使用符合间距系统的值（4px 的倍数） -->
<template>
  <div class="container" style="padding: 16px; margin: 8px;">
    <div style="margin-bottom: 16px;">内容</div>
  </div>
</template>
```

```css
/* ✅ 正确：定义间距工具类，统一管理 */
.spacing-xs { margin: 8px; }
.spacing-sm { margin: 12px; }
.spacing-md { margin: 16px; }
.spacing-lg { margin: 24px; }
.spacing-xl { margin: 32px; }
```

## 组件使用规范

### Element Plus 组件使用原则
- **优先使用 Element Plus 组件**：不要重复造轮子，充分利用组件库
- **保持组件使用一致性**：相同功能使用相同组件
- **合理使用组件属性**：充分利用组件提供的配置项，避免过度自定义

### 组件使用要求
- **表单组件**：统一使用 `el-form`、`el-input`、`el-select` 等
- **数据展示**：列表使用 `el-table`，卡片使用 `el-card`
- **反馈组件**：消息提示使用 `el-message`，通知使用 `el-notification`
- **导航组件**：菜单使用 `el-menu`，面包屑使用 `el-breadcrumb`

### 错误示例
```vue
<!-- ❌ 错误：自己实现按钮，而不是使用 Element Plus 组件 -->
<template>
  <button class="my-button" @click="handleClick">点击</button>
</template>
```

### 正确示例
```vue
<!-- ✅ 正确：使用 Element Plus 的 el-button 组件 -->
<template>
  <el-button type="primary" @click="handleClick">点击</el-button>
</template>
```

### 组件组合示例
```vue
<!-- ✅ 正确：合理组合使用 Element Plus 组件 -->
<template>
  <el-card>
    <template #header>
      <div class="card-header">
        <span>标题</span>
        <el-button type="primary" size="small">操作</el-button>
      </div>
    </template>
    <el-form :model="form" label-width="100px">
      <el-form-item label="名称">
        <el-input v-model="form.name" />
      </el-form-item>
    </el-form>
  </el-card>
</template>
```

## 交互规范

### 加载状态
- **必须提供加载反馈**：所有异步操作都要有加载状态
- **使用 Element Plus 的加载组件**：
  - 页面级加载：`v-loading` 指令
  - 按钮加载：`el-button` 的 `loading` 属性
  - 表格加载：`el-table` 的 `v-loading` 指令
  - 骨架屏：`el-skeleton` 组件

### 反馈机制
- **成功操作**：使用 `el-message.success()` 提示
- **错误操作**：使用 `el-message.error()` 提示，并显示错误详情
- **警告信息**：使用 `el-message.warning()` 或 `el-alert` 组件
- **重要通知**：使用 `el-notification` 组件

### 确认操作
- **危险操作必须二次确认**：删除、清空等操作使用 `el-message-box.confirm()`
- **使用 Element Plus 的确认框**：不要自己实现确认对话框

### 动画效果
- **页面切换**：使用 Vue Router 的过渡动画，淡入淡出效果
- **卡片悬停**：轻微阴影变化（`box-shadow` 过渡）
- **按钮点击**：使用 Element Plus 内置的按压效果
- **数据更新**：数字变化使用滚动动画（如 `countup.js`）

### 错误示例
```vue
<!-- ❌ 错误：没有加载状态，没有错误处理 -->
<template>
  <el-button @click="fetchData">加载数据</el-button>
  <div>{{ data }}</div>
</template>
<script setup>
const fetchData = async () => {
  const res = await api.getData(); // 没有加载状态，没有错误处理
  data.value = res.data;
}
</script>
```

### 正确示例
```vue
<!-- ✅ 正确：有加载状态、错误处理、成功反馈 -->
<template>
  <el-button :loading="loading" @click="fetchData">加载数据</el-button>
  <div v-loading="loading">{{ data }}</div>
</template>
<script setup>
import { ElMessage } from 'element-plus';

const loading = ref(false);
const data = ref(null);

const fetchData = async () => {
  loading.value = true;
  try {
    const res = await api.getData();
    data.value = res.data;
    ElMessage.success('数据加载成功');
  } catch (error) {
    ElMessage.error('数据加载失败：' + error.message);
  } finally {
    loading.value = false;
  }
}
</script>
```

### 确认操作示例
```vue
<!-- ✅ 正确：危险操作使用确认框 -->
<script setup>
import { ElMessageBox } from 'element-plus';

const handleDelete = async (id) => {
  try {
    await ElMessageBox.confirm('确定要删除吗？', '提示', {
      confirmButtonText: '确定',
      cancelButtonText: '取消',
      type: 'warning',
    });
    await api.delete(id);
    ElMessage.success('删除成功');
  } catch (error) {
    if (error !== 'cancel') {
      ElMessage.error('删除失败：' + error.message);
    }
  }
}
</script>
```

## 字体规范

- **主字体**: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto`
- **代码字体**: `'SF Mono', Monaco, 'Cascadia Code', monospace`
- **字号系统**: `12px` / `14px` / `16px` / `20px` / `24px` / `32px`
- **使用 Element Plus 的字体变量**：通过 CSS 变量使用，保持一致性

## 总结

在开发前端时，必须时刻谨记：
1. **布局**：参考 Element Plus 官方示例的布局方式
2. **配色**：严格遵循文档中的色彩系统
3. **间距**：使用统一的间距系统（4px 基础单位）
4. **组件**：合理使用 Element Plus 组件，保持一致性
5. **交互**：提供流畅的动画和及时的反馈
6. **科技感**：必须体现AI项目的科技属性，通过配色、视觉元素、动画等营造未来感和专业感

所有UI实现都应该遵循这些规范，确保界面美观、一致、易用，并具有符合AI项目的科技感。
