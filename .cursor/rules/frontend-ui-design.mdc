---
description: 编写或修改前端代码时生效（Vue组件、样式、交互等）
alwaysApply: true
---
# 前端UI/UX设计规范

## 设计原则

- **简洁高效**: 减少认知负担，提高操作效率
- **信息层次**: 重要信息突出，次要信息收敛
- **一致性**: 统一的视觉语言和交互模式
- **可访问性**: 遵循WCAG标准，确保良好的用户体验
- **科技感**: 体现AI项目的科技属性，营造未来感和专业感

## 布局规范

### 布局参考标准
- **必须参考 Element Plus 官方示例的布局方式**：学习官方文档中的布局最佳实践
- **整体布局结构**：
  - Header: Logo + 导航 + 用户信息
  - Sidebar: 菜单导航（可折叠）
  - Main Content: 页面主要内容区域
- **响应式设计**：
  - Mobile: < 768px（侧边栏自动收起）
  - Tablet: 768px - 1024px（自适应布局）
  - Desktop: > 1024px（完整布局）

### 布局实现要求
- 使用 Element Plus 的 `el-container`、`el-header`、`el-aside`、`el-main` 组件
- 保持页面内容区域的内边距统一（通常 16px 或 24px）
- 避免页面内容紧贴边缘，保持适当的留白

### 错误示例
```vue
<!-- ❌ 错误：直接使用 div，没有参考 Element Plus 布局组件 -->
<template>
  <div class="page">
    <div class="header">...</div>
    <div class="content">...</div>
  </div>
</template>
```

### 正确示例
```vue
<!-- ✅ 正确：使用 Element Plus 布局组件，参考官方示例 -->
<template>
  <el-container>
    <el-header>Header</el-header>
    <el-container>
      <el-aside width="200px">Sidebar</el-aside>
      <el-main>
        <!-- 页面内容，保持 16px 或 24px 内边距 -->
      </el-main>
    </el-container>
  </el-container>
</template>
```

## 配色规范

### 必须遵循文档中的色彩系统
- **主色调**:
  - Primary: `#1890ff` (蓝色，科技感)
  - Success: `#52c41a` (绿色，成功状态)
  - Warning: `#faad14` (橙色，警告)
  - Error: `#f5222d` (红色，错误)
- **中性色**:
  - Text Primary: `#262626`
  - Text Secondary: `#8c8c8c`
  - Border: `#d9d9d9`
  - Background: `#fafafa`

### 配色使用要求
- **禁止随意使用颜色**：所有颜色必须来自色彩系统
- **使用 Element Plus 的主题色变量**：通过 CSS 变量或主题配置使用
- **保持色彩一致性**：相同语义使用相同颜色（如所有成功提示都用 Success 色）

### 正确示例
```vue
<!-- ✅ 正确：使用 Element Plus 的 type 属性，自动应用主题色 -->
<el-button type="primary">主要按钮</el-button>
<el-button type="success">成功按钮</el-button>
<el-alert type="warning" :closable="false">警告信息</el-alert>
```

```css
/* ✅ 正确：使用 CSS 变量或主题配置 */
.custom-card {
  border-color: var(--el-border-color);
  background-color: var(--el-bg-color);
  color: var(--el-text-color-primary);
}
```

## 科技感设计规范

### 核心要求
- **必须体现AI项目的科技属性**：通过视觉元素、配色、动画等营造未来感和专业感
- **参考 vue-pure-admin 的设计风格**：vue-pure-admin 已经很好地体现了科技感设计，移植组件和布局时保持其设计风格
- **避免过度设计**：科技感应服务于功能，不应影响可用性

### 设计参考
- **参考 vue-pure-admin 的科技感实现**：
  - 配色方案：参考 vue-pure-admin 的主题配色
  - 视觉元素：参考其卡片设计、背景效果、图标使用
  - 动画效果：参考其微交互、过渡动画、加载效果
  - 布局特点：参考其卡片式设计、留白处理、层次感

### 实现方式
- **移植组件时保持设计风格**：从 vue-pure-admin 移植组件时，保持其原有的科技感设计
- **适配项目需求**：在保持科技感的基础上，根据项目实际需求进行微调
- **使用 Element Plus 组件**：优先使用 Element Plus 组件，通过样式定制实现科技感

### 禁止的设计
- ❌ 避免使用过于花哨的动画，影响性能
- ❌ 避免使用过多的装饰元素，影响内容阅读
- ❌ 避免使用过于鲜艳或刺眼的颜色
- ❌ 避免使用过时的设计风格（如拟物化设计）

## 间距规范

### 统一间距系统
- **基础单位**: `4px`
- **常用间距**: `8px`, `12px`, `16px`, `24px`, `32px`, `48px`
- **间距使用规则**:
  - 元素内部间距：8px、12px
  - 元素之间间距：16px、24px
  - 区块之间间距：24px、32px、48px

### 间距实现要求
- **使用 Element Plus 的间距工具类**：优先使用组件库提供的间距方案
- **保持间距一致性**：相同层级的元素使用相同间距
- **避免随意设置间距值**：所有间距值必须是 4px 的倍数

### 错误示例
```vue
<!-- ❌ 错误：随意设置间距值，不符合间距系统 -->
<template>
  <div style="padding: 13px; margin: 7px;">
    <div style="margin-bottom: 15px;">内容</div>
  </div>
</template>
```

### 正确示例
```vue
<!-- ✅ 正确：使用符合间距系统的值（4px 的倍数） -->
<template>
  <div class="container" style="padding: 16px; margin: 8px;">
    <div style="margin-bottom: 16px;">内容</div>
  </div>
</template>
```

```css
/* ✅ 正确：定义间距工具类，统一管理 */
.spacing-xs { margin: 8px; }
.spacing-sm { margin: 12px; }
.spacing-md { margin: 16px; }
.spacing-lg { margin: 24px; }
.spacing-xl { margin: 32px; }
```

## 组件使用规范

### Element Plus 组件使用原则
- **优先使用 Element Plus 组件**：不要重复造轮子，充分利用组件库
- **保持组件使用一致性**：相同功能使用相同组件
- **合理使用组件属性**：充分利用组件提供的配置项，避免过度自定义

### 组件使用要求
- **表单组件**：统一使用 `el-form`、`el-input`、`el-select` 等
- **数据展示**：列表使用 `el-table`，卡片使用 `el-card`
- **反馈组件**：消息提示使用 `el-message`，通知使用 `el-notification`
- **导航组件**：菜单使用 `el-menu`，面包屑使用 `el-breadcrumb`

### 错误示例
```vue
<!-- ❌ 错误：自己实现按钮，而不是使用 Element Plus 组件 -->
<template>
  <button class="my-button" @click="handleClick">点击</button>
</template>
```

### 正确示例
```vue
<!-- ✅ 正确：使用 Element Plus 的 el-button 组件 -->
<template>
  <el-button type="primary" @click="handleClick">点击</el-button>
</template>
```

### 组件组合示例
```vue
<!-- ✅ 正确：合理组合使用 Element Plus 组件 -->
<template>
  <el-card>
    <template #header>
      <div class="card-header">
        <span>标题</span>
        <el-button type="primary" size="small">操作</el-button>
      </div>
    </template>
    <el-form :model="form" label-width="100px">
      <el-form-item label="名称">
        <el-input v-model="form.name" />
      </el-form-item>
    </el-form>
  </el-card>
</template>
```

## 交互规范

### 加载状态
- **必须提供加载反馈**：所有异步操作都要有加载状态
- **使用 Element Plus 的加载组件**：
  - 页面级加载：`v-loading` 指令
  - 按钮加载：`el-button` 的 `loading` 属性
  - 表格加载：`el-table` 的 `v-loading` 指令
  - 骨架屏：`el-skeleton` 组件

### 反馈机制
- **成功操作**：使用 `el-message.success()` 提示
- **错误操作**：使用 `el-message.error()` 提示，并显示错误详情
- **警告信息**：使用 `el-message.warning()` 或 `el-alert` 组件
- **重要通知**：使用 `el-notification` 组件

### 确认操作
- **危险操作必须二次确认**：删除、清空等操作使用 `el-message-box.confirm()`
- **使用 Element Plus 的确认框**：不要自己实现确认对话框

### 动画效果
- **页面切换**：使用 Vue Router 的过渡动画，淡入淡出效果
- **卡片悬停**：轻微阴影变化（`box-shadow` 过渡）
- **按钮点击**：使用 Element Plus 内置的按压效果
- **数据更新**：数字变化使用滚动动画（如 `countup.js`）

### 错误示例
```vue
<!-- ❌ 错误：没有加载状态，没有错误处理 -->
<template>
  <el-button @click="fetchData">加载数据</el-button>
  <div>{{ data }}</div>
</template>
<script setup>
const fetchData = async () => {
  const res = await api.getData(); // 没有加载状态，没有错误处理
  data.value = res.data;
}
</script>
```

### 正确示例
```vue
<!-- ✅ 正确：有加载状态、错误处理、成功反馈 -->
<template>
  <el-button :loading="loading" @click="fetchData">加载数据</el-button>
  <div v-loading="loading">{{ data }}</div>
</template>
<script setup>
import { ElMessage } from 'element-plus';

const loading = ref(false);
const data = ref(null);

const fetchData = async () => {
  loading.value = true;
  try {
    const res = await api.getData();
    data.value = res.data;
    ElMessage.success('数据加载成功');
  } catch (error) {
    ElMessage.error('数据加载失败：' + error.message);
  } finally {
    loading.value = false;
  }
}
</script>
```

### 确认操作示例
```vue
<!-- ✅ 正确：危险操作使用确认框 -->
<script setup>
import { ElMessageBox } from 'element-plus';

const handleDelete = async (id) => {
  try {
    await ElMessageBox.confirm('确定要删除吗？', '提示', {
      confirmButtonText: '确定',
      cancelButtonText: '取消',
      type: 'warning',
    });
    await api.delete(id);
    ElMessage.success('删除成功');
  } catch (error) {
    if (error !== 'cancel') {
      ElMessage.error('删除失败：' + error.message);
    }
  }
}
</script>
```

## 字体规范

- **主字体**: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto`
- **代码字体**: `'SF Mono', Monaco, 'Cascadia Code', monospace`
- **字号系统**: `12px` / `14px` / `16px` / `20px` / `24px` / `32px`
- **使用 Element Plus 的字体变量**：通过 CSS 变量使用，保持一致性

## 参考 vue-pure-admin 项目

### 参考原则
- **按需移植**：只移植当前项目实际需要的功能，不要全量复制
- **保持简洁**：避免引入不必要的依赖和代码
- **适配项目**：移植的功能需要适配 AIHub 项目的实际需求

### 可参考的功能模块
- **工具函数**（`vue-pure-admin/src/utils/`）：HTTP 请求封装、权限验证、树形数据处理等
- **组件库**（`vue-pure-admin/src/components/`）：`ReCountTo`、`ReDialog`、`ReDrawer`、`ReQrcode` 等
- **布局系统**（`vue-pure-admin/src/layout/`）：布局结构、侧边栏、顶部导航、标签页
- **路由系统**（`vue-pure-admin/src/router/`）：路由配置、路由守卫、动态路由
- **状态管理**（`vue-pure-admin/src/store/`）：Pinia store 组织方式
- **指令**（`vue-pure-admin/src/directives/`）：`v-auth`、`v-copy`、`v-longpress`、`v-perms` 等
- **样式系统**（`vue-pure-admin/src/style/`）：主题配置、暗色模式、样式组织

### 移植流程
1. **需求分析**：明确需要什么功能
2. **查找参考**：在 vue-pure-admin 中找到对应的实现
3. **理解代码**：理解实现原理和依赖关系
4. **选择性移植**：只移植必要的代码和依赖
5. **适配改造**：根据项目需求进行适配和改造
6. **测试验证**：确保移植的功能正常工作

### 移植注意事项
- **依赖管理**：检查依赖、确保版本兼容、最小化依赖
- **代码适配**：适配 API 接口、类型定义、样式规范
- **测试验证**：功能测试、兼容性测试、性能测试

### 禁止的操作
- ❌ **禁止全量复制**：不要将整个 vue-pure-admin 项目复制到 AIHub 项目
- ❌ **禁止引入不必要的依赖**：不要为了移植功能而引入大量不必要的依赖
- ❌ **禁止破坏现有架构**：移植功能时不要破坏项目的现有架构和设计
- ❌ **禁止直接复制代码**：移植代码时需要理解并适配，不要直接复制粘贴

## 总结

在开发前端时，必须时刻谨记：
1. **布局**：参考 Element Plus 官方示例的布局方式
2. **配色**：严格遵循文档中的色彩系统
3. **间距**：使用统一的间距系统（4px 基础单位）
4. **组件**：合理使用 Element Plus 组件，保持一致性
5. **交互**：提供流畅的动画和及时的反馈
6. **科技感**：参考 vue-pure-admin 的科技感设计风格，移植组件时保持其设计特点
7. **参考项目**：按需移植 vue-pure-admin 的功能，保持项目简洁

所有UI实现都应该遵循这些规范，确保界面美观、一致、易用，并具有符合AI项目的科技感。

## 代码清理规范

- **废弃代码必须清理**：删除未使用的组件、样式、方法与注释代码，避免长期遗留
