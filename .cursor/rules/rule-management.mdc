---
alwaysApply: true
---

# 规范管理

## 规范添加流程

- **遇到用户经常强调的规范时，需主动询问是否添加到 rules 里**
- 当用户反复强调某个规范或最佳实践时，应该：
  1. 识别这是一个需要长期遵循的规范
  2. 主动询问用户是否要添加到 `.cursor/rules/` 目录下的规则文件中
  3. 如果用户同意，创建或更新相应的 `.mdc` 规则文件

## 规则文件组织

- 规则文件按类别分类，每个 `.mdc` 文件专注于一个主题
- 规则文件命名应该清晰，如：`java-code-style.mdc`、`logging.mdc`、`documentation.mdc`
- 规则内容应该简洁明确，包含：
  - 规则说明
  - 错误示例（如适用）
  - 正确示例（如适用）
