---
description: 开发新功能时生效
alwaysApply: false
---

# 功能开发规范

## 新功能开发检查清单

在完成新功能开发后，必须检查以下事项：

### 1. 菜单管理检查

- **检查是否需要新增菜单**：如果新功能需要在前端菜单中显示，必须添加菜单项
- **检查是否需要插入菜单数据**：菜单数据必须在数据库迁移脚本中插入，而不是手动添加

#### 菜单添加要求

1. **菜单数据位置**：菜单数据必须添加到对应的数据库迁移脚本中
   - 如果新功能有独立的迁移脚本（如 `V1.0.10__add_dict_tables.sql`），菜单数据应添加到该脚本中
   - 如果新功能是现有表的扩展，菜单数据应添加到对应的迁移脚本中

2. **菜单插入格式**：
   ```sql
   -- 插入菜单数据
   INSERT INTO menu (parent_id, name, path, component, icon, title, sort_order, show_link, keep_alive, status) VALUES
   ((SELECT id FROM (SELECT id FROM menu WHERE name = 'SystemManagement' LIMIT 1) AS tmp), 
    'SystemDict', '/system/dict/index', 'system/dict/index', 'ri:book-open-line', 'menus.pureDict', 5, 1, 0, 1)
   ON DUPLICATE KEY UPDATE name=VALUES(name);
   
   -- 为所有角色分配菜单权限
   INSERT INTO role_menu (role_id, menu_id)
   SELECT r.id, m.id
   FROM role r
   CROSS JOIN menu m
   WHERE r.is_deleted = 0 
     AND m.is_deleted = 0 
     AND m.name = 'SystemDict'
   ON DUPLICATE KEY UPDATE role_id=VALUES(role_id), menu_id=VALUES(menu_id);
   ```

3. **菜单字段说明**：
   - `parent_id`: 父菜单ID，0表示顶级菜单，或使用子查询获取父菜单ID
   - `name`: 菜单名称（路由name），必须唯一，使用驼峰命名（如 `SystemDict`）
   - `path`: 路由路径（如 `/system/dict/index`）
   - `component`: 组件路径（如 `system/dict/index`），父级菜单使用 `Layout`
   - `icon`: 图标名称（如 `ri:book-open-line`）
   - `title`: 菜单标题（i18n key，如 `menus.pureDict`）
   - `sort_order`: 排序（数字，越小越靠前）
   - `show_link`: 是否显示（0-隐藏，1-显示）
   - `keep_alive`: 是否缓存（0-不缓存，1-缓存）
   - `status`: 状态（0-禁用，1-启用）

4. **国际化支持**：菜单标题使用 i18n key，需要在 `frontend/locales/zh-CN.yaml` 和 `frontend/locales/en.yaml` 中添加对应的翻译

#### 错误示例

```sql
-- ❌ 错误：菜单数据没有添加到迁移脚本中，而是手动插入
-- 应该在迁移脚本中添加，而不是手动执行 SQL

-- ❌ 错误：菜单插入后没有为角色分配权限
INSERT INTO menu (...) VALUES (...);
-- 缺少：INSERT INTO role_menu ...

-- ❌ 错误：菜单标题没有使用 i18n key
INSERT INTO menu (..., title, ...) VALUES (..., '字典管理', ...);
-- 应该使用：'menus.pureDict'
```

#### 正确示例

```sql
-- ✅ 正确：在迁移脚本中添加菜单数据
-- ============================================
-- 添加字典管理菜单到系统管理下
-- ============================================
INSERT INTO menu (parent_id, name, path, component, icon, title, sort_order, show_link, keep_alive, status) VALUES
((SELECT id FROM (SELECT id FROM menu WHERE name = 'SystemManagement' LIMIT 1) AS tmp), 
 'SystemDict', '/system/dict/index', 'system/dict/index', 'ri:book-open-line', 'menus.pureDict', 5, 1, 0, 1)
ON DUPLICATE KEY UPDATE name=VALUES(name);

-- ✅ 正确：为所有角色分配菜单权限
INSERT INTO role_menu (role_id, menu_id)
SELECT r.id, m.id
FROM role r
CROSS JOIN menu m
WHERE r.is_deleted = 0 
  AND m.is_deleted = 0 
  AND m.name = 'SystemDict'
ON DUPLICATE KEY UPDATE role_id=VALUES(role_id), menu_id=VALUES(menu_id);
```

### 2. 国际化检查

- **检查是否需要添加 i18n 翻译**：如果菜单使用了 i18n key，必须在 `frontend/locales/zh-CN.yaml` 和 `frontend/locales/en.yaml` 中添加对应的翻译

### 3. 路由检查

- **检查路由配置**：如果新功能需要前端路由，检查路由是否正确配置
- **检查路由权限**：确保路由权限与菜单权限一致

### 4. API 接口检查

- **检查 API 接口文档**：重要功能应该更新 API 文档
- **检查接口权限**：确保接口权限配置正确

## 开发流程

1. **功能开发**：实现功能代码（后端 API、前端页面等）
2. **数据库迁移**：创建或更新数据库迁移脚本
3. **菜单检查**：检查是否需要添加菜单，如果需要，在迁移脚本中添加菜单数据
4. **权限分配**：在迁移脚本中为角色分配菜单权限
5. **国际化**：添加 i18n 翻译（如果需要）
6. **测试验证**：测试功能是否正常工作，菜单是否正确显示

## 注意事项

- **菜单数据必须通过迁移脚本管理**：不要手动插入菜单数据，确保数据库迁移的可重复性
- **使用 ON DUPLICATE KEY UPDATE**：避免重复插入菜单数据，使用 `ON DUPLICATE KEY UPDATE` 处理重复情况
- **菜单权限自动分配**：新菜单应该自动分配给所有角色（或根据业务需求分配），而不是手动分配
- **菜单命名规范**：菜单 `name` 字段使用驼峰命名，且必须唯一
