# 系统初始化文档

> 本文档说明系统初始化流程，包括数据库初始化、超级管理员创建等。

## 📚 相关文档

- [后端开发指南](./guide.md) - 返回后端文档总览
- [数据库设计文档](./database.md) - 查看数据库设计规范
- [数据库配置说明](./config.md) - 查看数据库连接配置
- [SQL 脚本管理](../sql/guide.md) - SQL脚本管理规范
- [功能清单](../features.md) - 查看功能规划与开发状态
- [项目主文档](../../README.md) - 返回项目文档入口

## 初始化流程

> **前置步骤**: 在开始初始化之前，请确保已完成：
> 1. 启动 Docker 开发环境（MySQL + Redis），参考 [Docker Compose 部署指南 - 本地开发](../deployment/docker-compose.md#场景二本地开发推荐)
> 2. 启动前后端服务（参考 [快速开始指南](./quick-start.md#4-启动前后端服务)）

**重要**: 
- 系统启动时会自动连接数据库（配置在 `application-dev.yml` 中），无需手动配置
- 所有环境（开发、测试、生产）使用统一的页面引导初始化方案

### 1. 数据库表结构初始化

#### 方式一：通过页面初始化（推荐）

1. 启动后端服务（系统会自动连接数据库）
2. 访问 `http://localhost:3000`，系统会自动跳转到 `/init` 页面
3. 如果数据库连接正常但表未初始化，会显示"初始化数据库表结构"按钮
4. 点击按钮，系统会自动执行 SQL 脚本创建所有表结构

**脚本位置**: `backend/aihub-api/src/main/resources/db/migration/V1.0.0__init_tables.sql`

#### 方式二：手动执行 SQL 脚本

如果需要手动执行，可以使用全量脚本：

```bash
mysql -u root -p < docs/sql/init/init_v1.0.0.sql
```

**全量脚本位置**: `docs/sql/init/init_v1.0.0.sql`（包含 CREATE DATABASE）

#### 方式三：使用 Flyway（推荐，开发环境已启用）

**开发环境已默认启用 Flyway**，应用启动时会自动执行迁移脚本。

**工作原理**：
- 首次启动：如果数据库为空，Flyway 会执行 `V1.0.0__init_tables.sql` 创建所有表
- 已有数据库：如果数据库已有表结构，Flyway 会创建基线（`baseline-on-migrate: true`），不会重复执行 V1.0.0
- 增量迁移：后续添加新字段时，只需创建新的迁移脚本（如 `V1.0.2__add_new_field.sql`），Flyway 会自动执行

**配置说明**: 详细配置请参考 [SQL 脚本管理文档 - Flyway 集成](../sql/guide.md#-flyway-集成推荐在开发环境启用)

**重要原则**: Flyway 只在开发环境启用，测试和生产环境必须禁用，由运维人员手动执行数据库迁移。

### 2. 超级管理员初始化

**重要**: 系统不会自动创建超级管理员，需要通过初始化页面手动创建。

#### 初始化流程

1. 完成数据库表结构初始化
2. 访问系统，自动跳转到初始化页面（`/init`）
3. 填写超级管理员信息：
   - 用户名：3-50个字符，不能为空
   - 邮箱：有效的邮箱地址
   - 密码：8-50个字符，建议包含大小写字母、数字和特殊字符
   - 确认密码：与密码一致
4. 提交创建，完成初始化
5. 自动跳转到首页

> **详细说明**：初始化页面的详细功能设计请参考 [前端初始化页面设计](#前端初始化页面设计) 部分

## Java 初始化服务实现

### 1. 初始化服务接口

**核心方法**:
- `isInitialized()`: 检查系统是否已初始化（是否存在超级管理员）
- `createSuperAdmin(dto)`: 创建超级管理员（仅在没有超级管理员时可用）

### 2. 初始化服务实现逻辑

**isInitialized() 逻辑**:
```
查询数据库中是否存在 role = 'SUPER_ADMIN' 且 is_deleted = 0 的用户
返回: true-已初始化, false-未初始化
```

**createSuperAdmin() 逻辑**:
```
1. 检查是否已存在超级管理员 -> 如果存在，抛出异常
2. 检查用户名是否已存在 -> 如果存在，抛出异常
3. 检查邮箱是否已存在 -> 如果存在，抛出异常
4. 使用 BCrypt 加密密码
5. 创建用户，设置 role = 'SUPER_ADMIN'
6. 保存到数据库
```

### 3. 初始化控制器

**API 接口**:
- `GET /api/init/status`: 检查系统是否已初始化
- `GET /api/init/database/status`: 检查数据库状态（连接状态、表结构初始化状态）
- `POST /api/init/database/init`: 初始化数据库表结构
- `POST /api/init/super-admin`: 创建超级管理员

**请求/响应**:
- 创建超级管理员请求: `{ username, email, password }`
- 响应: 统一结果格式 `Result<T>`

### 4. 初始化DTO

**字段**:
- `username`: 用户名（必填，3-50个字符）
- `email`: 邮箱（必填，邮箱格式验证）
- `password`: 密码（必填，8-50个字符）

### 5. 前端路由拦截

**拦截逻辑**:
```
1. 检查系统是否已初始化
   - 如果未初始化，跳转到 /init 页面（初始化页面）
   - 如果已初始化，正常访问

2. 排除路径：
   - /api/init/** - 初始化 API
   - /api/auth/login - 登录 API
   - /api/auth/refresh - 刷新 Token API
   - /init - 初始化页面
   - /login - 登录页面
   - /error - 错误页面
   - 静态资源路径
```

**路由守卫特性**：
- 支持网络错误处理，自动跳转到错误页面
- 支持系统初始化状态检查
- 防止循环跳转

## 超级管理员保护

### 删除保护

在用户删除逻辑中，需要检查是否为最后一个超级管理员：

**删除逻辑**:
```
1. 查询用户信息
2. 如果用户角色是 SUPER_ADMIN:
   - 查询当前超级管理员总数
   - 如果总数 <= 1，抛出异常（至少保留一个）
3. 执行逻辑删除
```

### 角色修改保护

在修改用户角色时，也需要保护最后一个超级管理员：

**角色修改逻辑**:
```
1. 查询用户信息
2. 如果从 SUPER_ADMIN 改为其他角色:
   - 查询当前超级管理员总数
   - 如果总数 <= 1，抛出异常（至少保留一个）
3. 更新用户角色
```

## 用户角色枚举

建议定义用户角色枚举，包含以下角色：

- `SUPER_ADMIN`: 超级管理员
- `ADMIN`: 管理员
- `USER`: 普通用户

## 安全检查清单

- [x] 数据库初始化脚本只创建表结构，不插入超级管理员
- [x] 提供初始化页面，通过页面创建第一个超级管理员
- [x] 初始化接口只能在没有超级管理员时调用
- [x] 删除用户时检查是否为最后一个超级管理员
- [x] 修改用户角色时检查是否为最后一个超级管理员
- [x] 禁用用户时检查是否为最后一个超级管理员（可选）
- [x] 初始化页面表单验证（用户名、邮箱、密码格式）

## 前端初始化页面设计

### 页面路径
- 初始化页面: `/init`
- API接口: `/api/init/*`

### 初始化页面功能（/init）

1. **数据库状态检查**
   - 页面加载时，调用 `GET /api/init/database/status` 检查数据库状态
   - 显示数据库连接状态、数据库是否存在、表结构是否已初始化
   - 如果数据库未连接或不存在，显示错误提示

2. **数据库表结构初始化**
   - 如果数据库连接正常但表未初始化，显示"初始化数据库表结构"按钮
   - 点击按钮，调用 `POST /api/init/database/init` 初始化表结构
   - 显示加载状态和初始化进度
   - 初始化完成后，自动刷新数据库状态

3. **初始化状态检查**
   - 数据库表结构初始化完成后，调用 `GET /api/init/status` 检查系统是否已初始化
   - 如果已初始化，自动跳转到首页
   - 如果未初始化，显示创建超级管理员表单

4. **创建超级管理员表单**
   - 用户名输入框（必填，3-50个字符）
   - 邮箱输入框（必填，邮箱格式验证）
   - 密码输入框（必填，8-50个字符，显示密码强度）
   - 确认密码输入框（必填，与密码一致验证）
   - 提交按钮

5. **表单验证**
   - 实时验证输入格式
   - 密码强度指示器（弱/中/强）
   - 提交前完整验证
   - 错误提示清晰明确

6. **提交处理**
   - 调用 `POST /api/init/super-admin` 创建超级管理员
   - 显示加载状态
   - 成功后显示成功提示
   - 自动跳转到首页

### 页面设计要点

- **科技感设计**: 体现AI项目的科技属性
- **简洁明了**: 表单布局清晰，操作简单
- **友好提示**: 清晰的说明文字和错误提示
- **响应式**: 支持不同屏幕尺寸

### Vue 3 组件实现要点

**组件结构**:
- 使用 Element Plus 的 `el-form` 组件
- 表单字段: 用户名、邮箱、密码、确认密码
- 密码强度指示器（弱/中/强）

**实现要点**:
- 页面加载时检查数据库状态和初始化状态（参考 [初始化页面功能](#初始化页面功能init) 部分）
- 表单验证：用户名（3-50字符）、邮箱（格式验证）、密码（8-50字符）、确认密码（一致性验证）
- 密码强度计算：根据长度、大小写、数字、特殊字符计算强度
- 提交处理：验证表单 → 调用 API → 显示加载状态 → 成功后跳转

## 初始化方案

**统一方案：页面引导初始化**

系统采用统一的页面引导初始化方案，适用于所有环境（开发、测试、生产）。

**优点**：
- ✅ **统一流程**：所有环境使用相同的初始化流程，降低学习成本
- ✅ **用户可控**：用户可以选择何时初始化，完全掌控初始化过程
- ✅ **安全性高**：超级管理员由用户自己创建，密码由用户设置
- ✅ **易于维护**：统一的流程，减少代码复杂度

**流程**：
1. 启动前后端服务（后端自动连接数据库）
2. 访问前端 → 自动跳转到 `/init` 页面
3. 点击"初始化数据库表结构"按钮（如果表未初始化）
4. 填写超级管理员信息并创建

**注意**：
- Docker 环境中的 MySQL 容器启动时会自动创建数据库
- 表结构和超级管理员需要通过页面初始化，确保所有环境的一致性

## 常见问题

### Q: 初始化页面无法访问怎么办？

A: 检查以下几点：
1. **后端服务是否启动**：
   - 检查控制台是否有错误
   - 确认服务监听在 `http://localhost:8080`

2. **前端服务是否启动**：
   - 检查前端服务是否运行
   - 确认访问地址是否正确（默认 `http://localhost:3000`）
   - 检查终端是否有错误信息

3. **数据库连接是否正常**：
   - 查看后端日志，检查是否有数据库连接错误
   - 参考 [数据库配置说明 - 常见问题](./config.md#常见问题)
   - 确认 Docker 容器是否正常运行

4. **查看应用日志**：
   - 查看控制台输出的错误信息
   - 检查是否有异常堆栈信息

5. **确认路由配置**：
   - 如果直接访问后端 API，确认路径是否正确
   - 前端路由是否正确配置

### Q: 初始化数据库表结构失败怎么办？

A: 检查以下几点：
1. **数据库连接是否正常**（参考 [数据库配置说明 - 常见问题](./config.md#常见问题)）
2. **数据库用户权限**：
   - 确保数据库用户有 `CREATE TABLE`、`ALTER TABLE` 等权限
   - 可以执行：`GRANT ALL PRIVILEGES ON aihub.* TO 'your_user'@'localhost';`
3. **表是否已存在**：
   - 如果表已存在，初始化会跳过（使用 `CREATE TABLE IF NOT EXISTS`）
   - 如果表结构有问题，可能需要先删除表
4. **查看应用日志**：
   - 查看后端日志中的具体错误信息
   - 检查 SQL 执行是否有错误

### Q: 如何创建新的超级管理员？

A: 使用现有的超级管理员账号登录，在用户管理页面创建新用户并设置为超级管理员角色。

### Q: 忘记超级管理员密码怎么办？

A: 可以通过以下方式重置：
1. **如果有其他超级管理员**：请其他管理员在用户管理页面重置密码
2. **如果没有其他管理员**：需要直接在数据库中重置（需要数据库访问权限）：
   ```sql
   -- 注意：密码需要使用 BCrypt 加密，不能直接设置明文密码
   -- 建议通过应用的重置密码功能，或创建新的超级管理员
   ```

### Q: 为什么不能删除最后一个超级管理员？

A: 系统要求至少保留一个超级管理员，以确保系统始终有管理员可以管理。这是安全保护机制。

### Q: 可以跳过初始化页面吗？

A: 不可以。系统要求必须通过初始化页面创建第一个超级管理员，这是安全设计，确保管理员信息由用户自己设置。

## 相关文档

- [数据库设计文档](./database.md) - 查看用户表结构
- [SQL 脚本管理](../sql/guide.md) - 查看SQL脚本管理规范
- [前端页面规划](../frontend/pages.md) - 查看初始化页面设计
- [Docker Compose 部署指南](../deployment/docker-compose.md) - Docker 部署相关配置
- [数据库配置说明](./config.md) - 数据库连接配置和常见问题