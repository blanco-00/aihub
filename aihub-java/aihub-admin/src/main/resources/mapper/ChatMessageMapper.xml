<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aihub.admin.mapper.ChatMessageMapper">

    <!-- 查询会话的消息列表 -->
    <select id="selectMessagesBySessionId" resultType="com.aihub.admin.dto.response.ChatMessageResponse">
        SELECT 
            id, session_id as sessionId, role, content, tokens,
            model_id as modelId, model_name as modelName,
            response_time_ms as responseTimeMs, created_at as createdAt
        FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC
    </select>

    <!-- 统计会话的消息数量 -->
    <select id="countBySessionId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM chat_message
        WHERE session_id = #{sessionId}
    </select>

</mapper>
