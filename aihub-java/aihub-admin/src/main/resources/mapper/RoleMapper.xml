<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aihub.admin.mapper.RoleMapper">

    <!-- 根据角色代码查询角色 -->
    <select id="selectByCode" resultType="com.aihub.admin.entity.Role">
        SELECT 
            id, code, name, description, status, created_at, updated_at, is_deleted
        FROM role 
        WHERE code = #{code} 
        AND is_deleted = 0
        LIMIT 1
    </select>

    <!-- 查询所有启用的角色 -->
    <!-- 注意：角色数据量通常很小（几个到几十个），且需要完整字段，因此查询所有字段 -->
    <select id="selectAllEnabled" resultType="com.aihub.admin.entity.Role">
        SELECT 
            id, code, name, description, status, created_at, updated_at, is_deleted
        FROM role 
        WHERE status = 1 
        AND is_deleted = 0
        ORDER BY id ASC
    </select>

</mapper>
